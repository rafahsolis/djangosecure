image: rafahsolis/djangosecure_runner:0.0.0

test:
  tags:
  - docker_python2.7

  script:
  - pip install -r requirements.txt
  - nosetests --with-coverage --cover-html --cover-inclusive --cover-package=djangosecure

  artifacts:
    paths:
    - cover